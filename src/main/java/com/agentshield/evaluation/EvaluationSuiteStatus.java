package com.agentshield.evaluation;

/** {@code SUPERSEDED} means a later version of the same suite name now exists; historical runs
 * against a superseded version remain reproducible — the version itself is never edited in place. */
public enum EvaluationSuiteStatus {
    ACTIVE,
    SUPERSEDED
}
